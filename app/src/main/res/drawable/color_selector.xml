<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:state_pressed="true">
        <color android:color="@android:color/white" />
    </item>
    <!-- Default state -->
    <item>
        <color android:color="@color/button" />
    </item>
</selector>
